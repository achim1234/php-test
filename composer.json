{
    "name": "local/foto-glitcher",
    "description": "A local digital image processing and glitch-art application.",
    "type": "project",
    "license": "proprietary",
    "require-dev": {
        "phpunit/phpunit": "^12.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": [
            "@test:php",
            "@test:js"
        ],
        "test:php": "phpunit",
        "test:js": "node --test tests/scripts.test.cjs"
    }
}
